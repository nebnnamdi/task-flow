import ResetPasswordForm from "./ResetPasswordForm";

export const metadata = {
  title: "Task-Flow | Reset Password",
  description: "Forgot Password",
};

const ResetPassword = () => {
  return <ResetPasswordForm />;
};

export default ResetPassword;
